#!/bin/zsh

mkdir $HOME/sec

# cd $HOME/sec

# TODO: Ideally, these wrappers/shims should automagically pull down the requirements/etc only as required
# Eg. The metasploit docker wrapper

# TODO: figure out nice shims to work well with pyenv and pyenv-virtualenv

# git clone git@github.com:NorthernSec/CVE-Scan.git cvescan