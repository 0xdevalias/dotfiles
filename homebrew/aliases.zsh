# Homebrew
alias brew-dir="cd $(brew --prefix) && pwd"
alias brew-cellardir="cd $(brew --prefix)/Cellar && pwd"
alias brew-cachedir="cd /Library/Caches/Homebrew && pwd"
alias brew-caskdir="cd $(brew --prefix)/Library/Taps/caskroom/homebrew-cask/ && pwd"